import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const perfilAccountSlice = createSlice({
  name: 'perfilAccount',
  initialState,
  reducers: {}
});

export const {} = perfilAccountSlice.actions

export default perfilAccountSlice.reducer